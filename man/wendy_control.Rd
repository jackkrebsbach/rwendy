% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{wendy_control}
\alias{wendy_control}
\title{Control parameters for solveWendy}
\usage{
wendy_control(
  optimize = TRUE,
  estimate_IC = FALSE,
  estimate_trajectory = FALSE,
  noise_sd = NA,
  compute_svd = TRUE,
  diag_reg = 1e-09,
  max_iterates = 100,
  test_fun_type = "MSG",
  radius_params = 2^(0:3),
  test_fun = "phi",
  S = 1,
  p = 16,
  include_boundary_layer = FALSE,
  n_bl = NULL,
  radius_min_time = NULL,
  radius_max_time = NULL,
  k_max = 200,
  max_test_fun_condition_number = 10000,
  min_test_fun_info_number = 0.95,
  min_number_points = 256,
  max_points_interp = 25,
  interpolation_method = NULL,
  fixed_radius = NULL,
  use_interp_uncertainty = TRUE,
  smoother = "erts",
  apply_fn = NULL
)
}
\arguments{
\item{optimize}{Logical. Run IRLS or MLE optimization to estimate parameters.}

\item{estimate_IC}{Logical. Estimate the initial condition.}

\item{estimate_trajectory}{Logical. Estimate the state trajectory using RTS.}

\item{noise_sd}{Numeric. Standard deviation of the noise, if known;
\code{NA} to estimate it from the data.}

\item{compute_svd}{Logical. Compute the SVD on the test function matrices (MSG).}

\item{diag_reg}{Numeric. Regularization for the covariance of the weak
residual, for stability.}

\item{max_iterates}{Integer. Maximum number of iterates for WENDy-IRLS.}

\item{test_fun_type}{Character. Test function type: Multi-scale Global
(\code{"MSG"}) or Single-scale Local (\code{"SSL"}).}

\item{radius_params}{Numeric vector. Radius factors to use in the MSG test
functions.}

\item{test_fun}{Character. \code{"phi"} for the \eqn{C^\infty} bump test
function \eqn{\Phi(t; r, n)}, or \code{"psi"} for the piecewise polynomial
test function \eqn{\Psi(t; r, p)}.}

\item{S}{Integer. Euler-Maclaurin series order expansion.}

\item{p}{Integer. Parameters in the piecewise polynomial test function
\eqn{\Psi(t; r, p)}.}

\item{include_boundary_layer}{Logical. Augment SSL test-function matrices
with boundary-layer rows + EM correction.}

\item{n_bl}{Integer. Boundary-layer test functions per side for the SSL
augmentation.}

\item{radius_min_time}{Numeric. User-set lower restriction on the test
function radius.}

\item{radius_max_time}{Numeric. User-set upper restriction on the test
function radius.}

\item{k_max}{Integer. The maximum number of test functions.}

\item{max_test_fun_condition_number}{Numeric. Truncate the SVD of the test
function matrices based on this condition number.}

\item{min_test_fun_info_number}{Numeric. Cumulative sum of the singular
values retained.}

\item{min_number_points}{Integer. Target number of data points after
interpolating.}

\item{max_points_interp}{Integer. Maximum number of points in the data to
interpolate.}

\item{interpolation_method}{Character. One of \code{"gp"},
\code{"poly_ls_N"}, \code{"spline"}, \code{"linear"}, \code{"cubic"},
\code{"loess"}, or \code{"kernel"}.}

\item{fixed_radius}{Integer. Fix the base test-function radius, bypassing
auto-selection.}

\item{use_interp_uncertainty}{Logical. If \code{TRUE}, weight the covariance
by interpolation uncertainty \eqn{W_{ii} = var_{ii} / \sigma^2}.}

\item{smoother}{Character. State smoother used by
\code{estimate_trajectory}: \code{"gp"} (Matern 5/2) or \code{"erts"}
(EKF/RTS).}

\item{apply_fn}{Function. Custom apply for multistart, e.g.
\code{parallel::mclapply} or \code{future.apply::future_lapply};
\code{NULL} uses \code{lapply}.}
}
\value{
A named list of control parameters for [solveWendy()].
}
\description{
Constructs the list of control parameters used by [solveWendy()].
Defaults shown in the usage are used for any argument not supplied.
}
