% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optim.R
\name{optimizeWendy}
\alias{optimizeWendy}
\title{Optimize a Pre-Built WENDy System}
\usage{
optimizeWendy(
  wendy_obj,
  p0 = NULL,
  method = attr(wendy_obj, "method"),
  control = list()
)
}
\arguments{
\item{wendy_obj}{A \code{wendy} object returned by \code{solveWendy} with
\code{control$optimize = FALSE}.}

\item{p0}{Numeric vector (single start) or matrix (multistart, one row per
starting point).  \code{NULL} triggers automatic equation-error
initialisation (same as \code{solveWendy}).}

\item{method}{Optimization method; defaults to the method stored in
\code{wendy_obj}.}

\item{control}{Optional list of control parameters to override those stored
in \code{wendy_obj}.}
}
\value{
A \code{wendy} object with \code{$phat} and \code{$data} populated.
  Multistart runs additionally attach \code{$multistart_results} and
  \code{$multistart_objectives}.
}
\description{
Run parameter optimization on a \code{wendy} object that was built with
\code{control = list(optimize = FALSE)}.  The expensive setup (symbolics,
test functions, covariance assembly) is reused; only the optimizer runs.
}
